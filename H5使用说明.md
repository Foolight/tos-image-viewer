# H5 图片浏览器使用说明

这是一个基于 Flask + HTML5 的火山引擎 TOS 图片浏览器。

## 功能特性

✅ 从火山引擎 TOS 获取所有图片
✅ 分页显示，每页 50 张图片
✅ 支持页码跳转（显示当前页前后2页）
✅ 点击图片放大查看
✅ 放大模式支持下载图片
✅ 响应式设计，支持手机和电脑
✅ 图片懒加载，优化性能

## 安装依赖

```bash
pip install -r requirements.txt
```

## 启动服务

```bash
python api.py
```

启动成功后会显示：

```
============================================================
火山引擎 TOS 图片浏览器
============================================================

Bucket: public-260224
Region: cn-beijing

服务启动中...
访问地址: http://localhost:5000
============================================================
```

## 访问页面

在浏览器中打开：

```
http://localhost:5000
```

## 使用方法

### 1. 浏览图片

- 页面会自动加载第 1 页的图片（每页 50 张）
- 图片以网格形式展示
- 鼠标悬停可查看文件名

### 2. 分页功能

- **上一页/下一页**：点击按钮切换页面
- **页码跳转**：点击具体页码快速跳转
- **页码显示**：显示当前页前后各 2 页，首页和尾页始终可见
- **页面信息**：顶部显示总图片数和当前页码

### 3. 查看大图

- 点击任意图片进入放大模式
- 显示图片完整尺寸
- 顶部显示文件名

### 4. 下载图片

- 在放大模式下，点击底部的"📥 下载图片"按钮
- 自动下载当前查看的图片
- 使用原始文件名

### 5. 退出放大模式

- 点击右上角 × 按钮
- 点击图片外的黑色区域
- 按 ESC 键

## 文件结构

```
huoshanTest/
├── api.py                  # Flask 后端 API
├── static/
│   └── index.html          # H5 前端页面
├── config.py               # 配置文件
├── upload_to_tos.py        # 图片上传脚本
└── requirements.txt        # 依赖包
```

## API 接口

### 1. 获取图片列表

```
GET /api/images?page=1&page_size=50
```

**参数：**
- `page`: 页码（从 1 开始，默认 1）
- `page_size`: 每页数量（1-100，默认 50）

**返回：**
```json
{
  "success": true,
  "data": {
    "images": [
      {
        "key": "image.jpg",
        "url": "https://bucket.endpoint/image.jpg",
        "size": 12345,
        "last_modified": "2024-02-24T12:00:00"
      }
    ],
    "pagination": {
      "current_page": 1,
      "page_size": 50,
      "total": 100,
      "total_pages": 2,
      "has_prev": false,
      "has_next": true
    }
  }
}
```

### 2. 生成下载链接

```
GET /api/download/<key>
```

**返回：**
```json
{
  "success": true,
  "download_url": "https://..."
}
```

## 注意事项

1. 确保 `config.py` 中的密钥配置正确
2. 确保有访问 TOS Bucket 的权限
3. 图片会按最后修改时间倒序排列（最新的在前）
4. 支持的图片格式：jpg, jpeg, png, gif, bmp, webp, svg
5. 下载链接有效期为 1 小时

## 移动端适配

- 自动适配手机屏幕
- 图片网格在手机上自动调整大小
- 支持触摸操作
- 禁用页面缩放，优化体验

## 性能优化

- 图片懒加载（loading="lazy"）
- 服务端分页，避免一次加载所有图片
- 页面切换时自动滚动到顶部
- CSS 动画使用硬件加速

## 故障排查

### 问题：页面显示"加载失败"

**解决方法：**
1. 检查 Flask 服务是否正常运行
2. 检查 `config.py` 中的密钥配置
3. 检查网络连接
4. 查看浏览器控制台错误信息

### 问题：图片不显示

**解决方法：**
1. 确认 TOS Bucket 中有图片
2. 检查 Bucket 的访问权限
3. 确认图片 URL 可以访问

### 问题：下载失败

**解决方法：**
1. 检查浏览器是否允许下载
2. 确认预签名 URL 未过期
3. 检查网络连接

## 自定义配置

### 修改每页显示数量

编辑 `static/index.html:300`：

```javascript
const pageSize = 50;  // 改成你想要的数量
```

### 修改端口

编辑 `api.py:155`：

```python
app.run(host='0.0.0.0', port=5000, debug=True)  # 修改 port
```

### 修改页码显示范围

编辑 `static/index.html:216-217`：

```javascript
let startPage = Math.max(1, current_page - 2);  // 当前页前2页
let endPage = Math.min(total_pages, current_page + 2);  // 当前页后2页
```

## 部署建议

### 本地开发

```bash
python api.py
```

### 生产环境

使用 gunicorn 或 uwsgi：

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 api:app
```

## 技术栈

- **后端**：Flask + TOS Python SDK
- **前端**：原生 HTML5 + CSS3 + JavaScript
- **图片存储**：火山引擎 TOS

## 许可

此项目仅供学习和内部使用。
