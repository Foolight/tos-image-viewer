# 免费部署到云平台指南

## 方案1：Render（推荐）

### 优点
- ✅ 完全免费
- ✅ 自动提供 HTTPS
- ✅ 从 GitHub 自动部署
- ✅ 无需信用卡
- ✅ 操作简单

### 部署步骤

#### 1. 准备代码

**先确保本地有图片数据**：
```bash
# 如果还没有运行过同步脚本
python sync_images_folder.py
```

这会生成 `images_data.json` 文件。

#### 2. 上传到 GitHub

**创建新仓库**：
1. 访问：https://github.com/new
2. 仓库名称：`tos-image-viewer`（或其他名称）
3. 设为私有仓库（Private）
4. 点击 "Create repository"

**上传代码**：

在 `huoshanTest` 文件夹中运行：

```bash
# 初始化 Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 连接到 GitHub（替换 YOUR_USERNAME 为你的GitHub用户名）
git remote add origin https://github.com/YOUR_USERNAME/tos-image-viewer.git

# 推送
git branch -M main
git push -u origin main
```

#### 3. 部署到 Render

1. **访问 Render**：https://render.com
2. **注册/登录**：可以使用 GitHub 账号登录
3. **创建新服务**：
   - 点击 "New +" → "Web Service"
   - 连接你的 GitHub 仓库
   - 选择 `tos-image-viewer` 仓库

4. **配置服务**：
   - **Name**: `tos-image-viewer`（或其他名称）
   - **Region**: Singapore（新加坡，离中国最近）
   - **Branch**: `main`
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn api_v2:app`
   - **Instance Type**: `Free`

5. **部署**：
   - 点击 "Create Web Service"
   - 等待部署完成（约 3-5 分钟）

6. **访问你的网站**：
   - 部署成功后会显示 URL，类似：`https://tos-image-viewer.onrender.com`

### 更新图片

当你添加新图片后：

```bash
# 1. 同步图片（生成新的 images_data.json）
python sync_images_folder.py

# 2. 提交更新
git add images_data.json
git commit -m "Update images"
git push

# 3. Render 会自动重新部署
```

---

## 方案2：Railway

### 优点
- ✅ 免费额度充足（500小时/月）
- ✅ 速度快
- ✅ 支持自定义域名

### 部署步骤

1. **访问 Railway**：https://railway.app
2. **登录**：使用 GitHub 账号
3. **新建项目**：
   - 点击 "New Project"
   - 选择 "Deploy from GitHub repo"
   - 选择你的仓库
4. **配置**：
   - Railway 会自动检测 Python 项目
   - 自动安装依赖
5. **生成域名**：
   - 在设置中点击 "Generate Domain"
   - 获得类似 `xxx.up.railway.app` 的域名

---

## 方案3：Vercel（静态托管）

如果你只想托管前端，API在本地运行：

1. **访问 Vercel**：https://vercel.com
2. **登录**：GitHub 账号
3. **导入项目**
4. **设置**：
   - Framework Preset: `Other`
   - Root Directory: `static`

**注意**：这种方式只托管前端，API需要另外部署。

---

## 方案4：Replit（最简单，无需Git）

### 优点
- ✅ 无需 GitHub
- ✅ 在线IDE
- ✅ 即时部署

### 部署步骤

1. **访问 Replit**：https://replit.com
2. **创建新 Repl**：
   - 点击 "+ Create"
   - 选择 "Import from GitHub"（或直接创建Python项目）
3. **上传文件**：
   - 直接拖拽所有文件到编辑器
4. **配置**：
   - 创建 `.replit` 文件：
     ```
     run = "python api_v2.py"
     ```
5. **运行**：
   - 点击 "Run"
   - Replit 会自动提供一个公开URL

---

## 选择建议

| 平台 | 推荐度 | 适合场景 |
|------|--------|----------|
| **Render** | ⭐⭐⭐⭐⭐ | 最推荐，稳定可靠 |
| **Railway** | ⭐⭐⭐⭐ | 速度快，但可能需要绑卡 |
| **Replit** | ⭐⭐⭐ | 最简单，但性能一般 |
| **Vercel** | ⭐⭐ | 只适合静态站点 |

## 重要提示

### 1. 数据持久化

免费平台可能会在一段时间不活动后重启服务，导致 `images_data.json` 丢失。

**解决方法**：
- 定期备份 `images_data.json` 到 GitHub
- 或者使用云数据库（如 MongoDB Atlas 免费版）

### 2. 文件上传

如果你想在部署后继续上传图片：

**方法A**：本地上传，然后推送到 GitHub
```bash
python upload_to_tos.py  # 上传到TOS
python sync_images_folder.py  # 同步URL列表
git add images_data.json
git commit -m "Add new images"
git push  # 自动重新部署
```

**方法B**：直接使用 API 添加 URL
```bash
curl -X POST https://your-app.onrender.com/api/add_image \
  -H "Content-Type: application/json" \
  -d '{"url": "https://public-260224.tos-cn-beijing.volces.com/new-image.jpg"}'
```

### 3. 自定义域名

大多数免费平台支持绑定自定义域名：
- **Render**: Settings → Custom Domains
- **Railway**: Settings → Domains
- **Vercel**: Project Settings → Domains

---

## 故障排查

### 问题：部署失败

**检查**：
1. `requirements.txt` 是否正确
2. `Procfile` 是否存在
3. 查看部署日志

### 问题：页面显示404

**检查**：
1. 确认 `static/index.html` 存在
2. 检查路由配置

### 问题：图片不显示

**检查**：
1. `images_data.json` 是否存在且有数据
2. 图片URL是否可以公开访问
3. 浏览器控制台是否有CORS错误

---

## 快速开始（Render部署）

```bash
# 1. 同步图片URL
python sync_images_folder.py

# 2. 初始化Git
git init
git add .
git commit -m "Initial commit"

# 3. 上传到GitHub
git remote add origin https://github.com/YOUR_USERNAME/tos-image-viewer.git
git push -u origin main

# 4. 访问 https://render.com
# 5. 点击 "New +" → "Web Service"
# 6. 连接GitHub仓库
# 7. 使用默认配置，点击 "Create Web Service"
# 8. 等待部署完成

# 完成！访问你的网站
```

---

## 成本

所有推荐的平台都提供免费套餐，对于这个项目完全够用：

- **Render**: 750小时/月（永久免费）
- **Railway**: 500小时/月（需绑卡但不扣费）
- **Replit**: 无限制（但性能有限）
- **Vercel**: 无限制（仅静态）

**推荐使用 Render**，无需绑卡，完全免费！
