# 快速部署到免费云平台

## 最简单的方法（推荐）

### 步骤1：同步图片

```bash
python sync_images_folder.py
```

这会扫描 `Images` 文件夹并生成 `images_data.json`

### 步骤2：运行部署脚本

```bash
deploy.bat
```

按提示操作：
1. 输入提交信息（可直接回车使用默认）
2. 输入 GitHub 仓库地址

**GitHub 仓库地址获取方式**：
1. 访问：https://github.com/new
2. 仓库名称：`tos-image-viewer`
3. 选择 **Private**（私有）
4. 点击 "Create repository"
5. 复制仓库地址：`https://github.com/YOUR_USERNAME/tos-image-viewer.git`

### 步骤3：部署到 Render

1. 访问：https://render.com
2. 使用 GitHub 账号登录（Sign up with GitHub）
3. 点击 **"New +"** → **"Web Service"**
4. 点击 **"Connect account"** 连接 GitHub
5. 找到并选择 `tos-image-viewer` 仓库
6. 配置：

   | 选项 | 值 |
   |------|-----|
   | Name | `tos-image-viewer` |
   | Region | `Singapore` |
   | Branch | `main` |
   | Runtime | `Python 3` |
   | Build Command | `pip install -r requirements.txt` |
   | Start Command | `gunicorn api_v2:app` |
   | Instance Type | **Free** |

7. 点击 **"Create Web Service"**
8. 等待部署（约 3-5 分钟）

### 完成！

部署成功后，Render 会显示你的网站地址：
```
https://tos-image-viewer-xxx.onrender.com
```

访问这个地址就能看到你的图片浏览器了！

---

## 更新图片

### 方法1：添加新图片后更新

```bash
# 1. 将新图片放入 Images 文件夹
# 2. 运行上传脚本
python upload_to_tos.py

# 3. 同步URL
python sync_images_folder.py

# 4. 推送更新
git add images_data.json
git commit -m "Add new images"
git push
```

Render 会自动检测到更新并重新部署（约 1-2 分钟）

### 方法2：直接通过 API 添加

```bash
curl -X POST https://your-app.onrender.com/api/add_image \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://public-260224.tos-cn-beijing.volces.com/new-image.jpg"
  }'
```

---

## 手动部署（不使用脚本）

如果 `deploy.bat` 无法运行，可以手动执行：

```bash
# 1. 初始化 Git
git init
git branch -M main

# 2. 添加文件
git add .

# 3. 提交
git commit -m "Initial commit"

# 4. 连接 GitHub（替换为你的仓库地址）
git remote add origin https://github.com/YOUR_USERNAME/tos-image-viewer.git

# 5. 推送
git push -u origin main
```

然后按照"步骤3"在 Render 上部署。

---

## 常见问题

### Q: 提示 "git not found"？

**A:** 需要安装 Git：
1. 下载：https://git-scm.com/download/win
2. 安装（一路点击 Next）
3. 重新打开命令行

### Q: 推送到 GitHub 需要密码？

**A:** 从 2021 年起 GitHub 不再支持密码，需要使用 Personal Access Token：
1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 `repo`
4. 生成后复制 token
5. 在推送时输入 token（而不是密码）

### Q: Render 部署失败？

**A:** 检查：
1. `requirements.txt` 是否存在
2. `Procfile` 是否存在
3. 查看 Render 的部署日志（Logs）

### Q: 网站可以访问但没有图片？

**A:** 检查：
1. `images_data.json` 是否已推送到 GitHub
2. 文件中是否有图片数据
3. 图片URL是否可以公开访问

### Q: Render 免费版有限制吗？

**A:**
- ✅ 永久免费
- ✅ 自动 HTTPS
- ⚠️ 15分钟无访问会休眠（首次访问会慢一点）
- ⚠️ 每月 750 小时限制（对个人项目足够）

---

## 自定义域名（可选）

如果你有自己的域名：

1. 在 Render 项目设置中点击 **"Custom Domains"**
2. 添加你的域名（如 `images.yourdomain.com`）
3. 在域名 DNS 设置中添加 CNAME 记录：
   - Name: `images`
   - Value: Render 提供的地址
4. 等待 DNS 生效（可能需要几分钟到几小时）

---

## 备份数据

定期备份 `images_data.json`：

```bash
# 下载当前的图片列表
curl https://your-app.onrender.com/api/images?page_size=1000 > backup.json
```

---

## 成本

**完全免费！**

- Render Free 套餐：750 小时/月
- GitHub Free：私有仓库无限
- 火山引擎 TOS：按使用量付费（图片存储和流量）

对于个人项目，每月成本几乎为 0。

---

## 下一步

部署成功后，你可以：

1. ⭐ 分享链接给朋友
2. 📱 添加到手机桌面（PWA）
3. 🎨 自定义 `static/index.html` 修改样式
4. 🔐 添加密码保护（修改 `api_v2.py`）
5. 📊 添加访问统计
6. 🌐 绑定自定义域名

---

**遇到问题？** 查看 [部署指南.md](部署指南.md) 获取更多详细信息！
